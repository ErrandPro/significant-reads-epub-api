name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf --max-time 10 --retry 2 https://significant-reads-epub-api.onrender.com/health || exit 1
